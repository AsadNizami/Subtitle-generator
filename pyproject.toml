[project]
name = "srtgen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "whisperx",
    "torch<2.4",
    "transformers",
    "torchaudio<2.4",
]
